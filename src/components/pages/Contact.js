import React from "react";
import ContactTitle from "../ContactTitle/ContactTitle";

function Contact() {
    return (
        <div>
            <ContactTitle />
        </div>
    );
};

export default Contact;