import React from "react";
import ProjectTitle from "../ProjectTitle/ProjectTitle";

function Projects() {
    return (
        <div>
            <ProjectTitle />
        </div>
    );
};

export default Projects;